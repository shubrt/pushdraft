{
  "name": "pushdraft",
  "version": "0.0.1",
  "description": "CLI for the open-source postplan.dev alternative with authentication-protected HTML drafts.",
  "keywords": [
    "cli",
    "html",
    "preview",
    "publishing"
  ],
  "homepage": "https://pushdraft.dev",
  "bugs": "https://github.com/shubrt/pushdraft/issues",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shubrt/pushdraft.git",
    "directory": "apps/cli"
  },
  "bin": {
    "pushdraft": "./dist/cli.js"
  },
  "files": [
    "dist/"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target node --format esm",
    "prepack": "bun run build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@pushdraft/contracts": "workspace:*",
    "@tool/tsconfig": "workspace:*",
    "@types/bun": "^1.3.4",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22"
  }
}
